using Player;

namespace Core
{
    public class PlayerModelBootstrapperSignal
    {
        public PlayerModelBootstrapper Bootstrapper;

        public PlayerModelBootstrapperSignal(PlayerModelBootstrapper modelBootstrapper)
        {
            Bootstrapper = modelBootstrapper; 
        }
    }
}